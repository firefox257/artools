// main-page.js
//var observable = require("@nativescript/core/data/observable");
var pageModule = require("@nativescript/core/ui/page");

/*
var messageLabel; // Variable to hold the reference to the Label

// Function called when the page is loaded
function onPageLoaded(args) {
    var page = args.object;
    // Get a reference to the Label using its ID
    messageLabel = page.getViewById("messageLabel");
}
exports.onPageLoaded = onPageLoaded;

// Function called when the button is tapped
function onChangeMessageTap(args) {
    var button = args.object;
     // Change the text of the messageLabel
    if (messageLabel) {
        messageLabel.text = "Message changed by button!";
    }
}
exports.onChangeMessageTap = onChangeMessageTap;
*/